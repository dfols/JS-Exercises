function convertTemperature(temp, unit) {

  // if unit is c; convert temp to celcius and return
  // if unit is f; convert temp to fahrenheit and return
  // if unit is not c or f; alert that unit is invalid

}

let tempToConvert = // get input from user
let unitToConvertTo = // get input from user

// run convertTemperature fucntion with user temp and unit
// store returned value in convertedTemp
let convertedTemp = convertTemperature(tempToConvert, unitToConvertTo);

// output
console.log(convertedTemp);